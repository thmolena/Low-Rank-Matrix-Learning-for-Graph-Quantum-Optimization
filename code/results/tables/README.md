# Numerical tables

The manuscript's three tables are rendered from the CSV and JSON files in the
parent directory. `split_comparison.csv`, `temperature_sweep.csv`,
`source_manifest.csv`, and `runtime.csv` contain every numerical entry quoted
in the paper.
